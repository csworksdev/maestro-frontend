# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        # with:
        #   repoToken: ${{ secrets.GITHUB_TOKEN }}
        #   firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAESTRO_FRONTEND }}
        #   channelId: live
        #   projectId: maestro-front
      - env:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAESTRO_FRONTEND }}  # Use the Firebase token from GitHub Secrets
          channelId: live
          projectId: maestro-front
        run: npx firebase-tools@latest deploy --only hosting --project maestro-front
